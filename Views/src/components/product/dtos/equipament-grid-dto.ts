import { ProductDto } from "./product-dto";

export class EquipamentGridDto {
  productId:number;
  name: string;
  manufacturer: string;
  segment: string;
  model: string;
  description: string;
  length: number;
  // entityComplete:ProductDto;
}
