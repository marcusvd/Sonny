import { AutoRowsDirectiveDirective } from './auto-rows-directive.directive';

describe('AutoRowsDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoRowsDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
