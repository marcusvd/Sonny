export class PaginatorDto {
  // length: number;
  // pageIndex: number;
  // pageSize: number;
  // previousPageIndex: number;

  currentPg: number;
  totalPgs: number;
  pgSize: number;
  totalCount: number;
  hasPrevious:boolean;
  hasNext:boolean;

}
